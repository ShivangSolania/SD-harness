# Project Patterns

> Positive patterns the cluster should follow. Promoted by `agents/reflection.md` when the same anti-pattern is corrected 3+ times.
> Cross-reference with `knowledge/patterns/hermes-reasoning-patterns.md` for deep-reasoning heuristics.

## Format

```markdown
### [ISO timestamp] — <pattern name>
- **Context:** <when this applies>
- **Pattern:** <what to do>
- **Why:** <rationale>
```

(append new entries below this line — most recent first)
