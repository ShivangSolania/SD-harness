# Build artifacts
node_modules
package.json
package-lock.json
bun.lock
tui.json

# Harness runtime config (per-installation, do not commit)
harness.jsonc
harness.jsonc.*.tmp

# Themes (user-installed)
themes

# Sandboxed runtime state (regenerated per session)
sandbox/experiments/.thought.md
sandbox/temporary/*
!sandbox/temporary/.gitkeep

# Observability (rewritten on each run)
observability/failures.json

# Per-session retry budget
memory/session/state.json
